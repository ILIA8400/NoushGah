@model List<OrderWrapper>

<body>

    <partial name="_Preload" />

    <div class="header">
        <div class="title-header-bar fixed-top bg-white">
            <a href="#" class="back-btn"><i class="icon-right"></i></a>
            <h1>نمایش سفارشات</h1>
            <span class="btn-sidebar">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M3 7H21" stroke="#033f38" stroke-width="1.5" stroke-linecap="round"></path>
                    <path d="M3 12H21" stroke="#033f38" stroke-width="1.5" stroke-linecap="round"></path>
                    <path d="M3 17H21" stroke="#033f38" stroke-width="1.5" stroke-linecap="round"></path>
                </svg>
            </span>
        </div>
    </div>

    <section class="container" style="margin-top:100px">
        <h2 class="mb-3">سفارش‌های در حال آماده‌سازی</h2>
        <div class="row" id="ordersContainer">
            <div class="col-md-4 mb-3">
                @foreach(var order in Model){
                    <div class="card p-3 shadow-sm rounded-3">
                        <h5>سفارش # @order.OrderId</h5>
                        <ul class="mt-1">
                            @foreach (var item in order.BasketItems)
                            {
                                <li>@item.Product.Name <span style="font-weight: bold; color: #d35400; margin-left: 5px;">×@item.Count</span></li>
                            }
                        </ul>
                    </div>
                }
            </div>
        </div>
    </section>

    <partial name="_MenuBarFooter" />

    <script type="text/javascript" src="~/Scripts/bootstrap.min.js"></script>
    <script type="text/javascript" src="~/Scripts/jquery.min.js"></script>
    <script type="text/javascript" src="~/Scripts/swiper-bundle.min.js"></script>
    <script type="text/javascript" src="~/Scripts/bootstrap-select.min.js"></script>
    <script type="text/javascript" src="~/Scripts/carousel.js"></script>
    <script type="text/javascript" src="~/Scripts/sidebar.js"></script>
    <script type="text/javascript" src="~/Scripts/main.js"></script>

@*     <script>
        document.addEventListener("DOMContentLoaded", function () {
            const orders = [
                { id: 1, customer: "علی", items: ["لاته", "کیک شکلاتی"] },
                { id: 2, customer: "مریم", items: ["اسپرسو", "مافین بلوبری"] }
            ];

            const container = document.getElementById("ordersContainer");

            orders.forEach(order => {
                const orderCard = document.createElement("div");
                orderCard.className = "col-md-4 mb-3";
                orderCard.innerHTML = `
                    <div class="card p-3 shadow-sm rounded-3">
                        <h5>سفارش #${order.id} - ${order.customer}</h5>
                        <ul class="mt-1">
                              ${order.items.map(item => `<li>${item} <span style="font-weight: bold; color: #d35400; margin-left: 5px;">×2</span></li>`).join("")}
                        </ul>
                    </div>
                `;
                container.appendChild(orderCard);
            });
        });
    </script> *@

</body>